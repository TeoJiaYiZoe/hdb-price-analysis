# Frontend (Vite + React)

## Setup

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` → `http://localhost:8000`. Ensure the backend is running and `backend/model/full_pipeline.pkl` exists.
